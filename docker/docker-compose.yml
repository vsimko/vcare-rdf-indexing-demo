version: '2'

services:
  solr:

    # see also https://hub.docker.com/_/solr/
    image: solr:alpine

    ports:
     - "8983:8983"

    volumes:
      - data:/opt/solr/server/solr/mycores

    entrypoint:
      - docker-entrypoint.sh
      - solr-precreate
      - mycore

volumes:
  data: